class Pet {
    id: Int;
    name: Int;
    tag2: string;
    constructor(id: Int, name: Int, tag2: string) {
    }
}
class Error {
    code: Int;
    message: Int;
    constructor(code: Int, message: Int) {
    }
}
